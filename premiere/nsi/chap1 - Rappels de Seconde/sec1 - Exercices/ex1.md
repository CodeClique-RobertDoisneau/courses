# Exercices de révision
## 1 - Variables, Types et Opérations
### ⭐ **<u>Exercice 1</u>** : *Échange de valeurs* 
Créez deux variables `a = 5` et `b = 10`. Écrivez les lignes de code nécessaires pour échanger les valeurs des deux variables (la valeur de a doit devenir 10 et celle de b doit devenir 5).
```python
```
### ⭐ **<u>Exercice 2</u>** : *Les types* 
Déterminez le type de chaque variable suivante sans exécuter le code :
`x = "15"`
`y = 15`
`z = 15.0`
`a = True`

### ⭐⭐ **<u>Exercice 3</u>** : *Calcul* 
Demandez à l'utilisateur son année de naissance, calculez son âge approximatif (en supposant que nous sommes en 2026) et affichez le résultat avec une phrase : "Vous avez environ X ans."
```python
```

## 2 - Conditions et Boucles
### ⭐ **<u>Exercice 1</u>** : *Conditions*
Écrivez un programme qui demande un nombre à l'utilisateur. Si le nombre est positif, affichez "Positif". S'il est nul, affichez "Nul". Sinon, affichez "Négatif".
```python
```
### ⭐⭐ **<u>Exercice 2</u>**: *Boucle non bornée*
Écrivez un programme qui demande à l'utilisateur de deviner un nombre secret (choisissez secret = 7). Tant que l'utilisateur n'a pas trouvé le nombre, le programme lui demande de réessayer. Quand il trouve, affichez "Bravo !".
```python
```
### ⭐⭐ **<u>Exercice 3</u>** : *Boucle bornée*
Utilisez une boucle `for` pour afficher tous les nombres pairs entre 0 et 20 inclus.
```python
```

## 3 - Les Fonctions
### ⭐⭐ **<u>Exercice 1</u>** : *Création de fonction* 
Définissez une fonction nommée `aire_rectangle` qui prend deux paramètres, `longueur` et `largeur`, et qui retourne le résultat de la multiplication des deux.
```python
```
### ⭐ **<u>Exercice 2</u>** : *Appel de fonction* 
En utilisant la fonction créée ci-dessus, écrivez un programme qui demande à l'utilisateur les dimensions d'un rectangle, appelle la fonction `aire_rectangle`, et affiche le résultat dans la console.
```python
```

## 4 - Listes, Chaînes et Dictionnaires
### ⭐ **<u>Exercice 1</u>** : *Manipulation de listes* 
Soit la liste 
```python
fruits = ["pomme", "banane", "poire"]
```
1. Ajoutez "fraise" à la fin de la liste.
```python
```
2. Affichez le deuxième élément de la liste.
```python
```
3. Modifiez le premier élément pour qu'il soit "orange".
```python
```
### ⭐⭐ **<u>Exercice 2</u>** :* Chaînes de caractères*
Soit la variable 
```python
phrase = "Programmer c'est génial".
```
1. Affichez uniquement les 5 premiers caractères ("Progr").
```python
```
2. Transformez toute la chaîne en majuscules (cherchez la méthode Python appropriée).
```python
```
### ⭐⭐ **<u>Exercice 3</u>** : *Dictionnaires* 
1. Créez un dictionnaire nommé `contact` qui contient les clés suivantes : `"nom": "Dupont"`,` "age": 25`,` "ville": "Paris"`.
```python
```
2. Affichez la valeur associée à la clé `"ville"`.
```python
```
3. Modifiez l'âge pour qu'il soit 26.
```python
```
4. Ajoutez une nouvelle clé `"profession"` avec la valeur `"étudiant"`.
```python
```
